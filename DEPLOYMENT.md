# 云端部署状态

网页读取云端 D1 数据库，没有本地每日任务。没有成功接入云端调度前明确显示“初始快照”。

云端抓取程序将论文 JSON POST 到 `/api/ingest`，Authorization 使用 Bearer INGEST_TOKEN。在托管环境设置该密钥；若托管平台要求站点登录，调度器也须配置对应的站点访问授权。不要将密钥放入前端。

调度器每日在云端执行论文抓取，再提交 `{papers: [...], errors: [...]}`。仅上传已核实会议来源的记录。D1 保存结果，前端加载 `/api/papers`。没有注册云端调度器就不算完成每日自动更新。

原来的 Windows 计划任务已撤销。开发文件仅供开发，不会每日运行。
